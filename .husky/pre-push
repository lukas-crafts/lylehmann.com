#!/bin/bash
# Pre-push hook - runs build check only, release is handled by GitHub Actions
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo "On main branch - ensure build passes before pushing..."
fi